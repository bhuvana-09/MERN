# Server Port
PORT=5000

# MongoDB Connection URI
MONGO_URI=mongodb://127.0.0.1:27017/subscription-manager

# Secret key for signing JWTs (must be kept secret in production)
JWT_SECRET=dev_jwt_secret_key_12345

# Allowed Client URL for CORS
CLIENT_URL=http://localhost:5173

# Node Environment
NODE_ENV=development
